<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: Compose draws everything, so the platform theme only needs
         to hand off a matching window background to avoid a flash on launch. -->
    <style name="Theme.LocalNews" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Core SplashScreen API: the system splash owns the very first frame. -->
    <style name="Theme.LocalNews.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.LocalNews</item>
    </style>
</resources>
